name: Run Script

on:
  push:
    branches:
      - main
      - feature/*

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run script
        run: |
          echo "Running script..."
          cat file.txt
